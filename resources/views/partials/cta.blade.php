<section class="section-space-sm">
    <div class="container">
        <div class="cta-panel reveal">
            <span class="section-label">Your move</span>
            <h2>{{ $title ?? 'Ready to build something people remember?' }}</h2>
            <p class="mb-4">{{ $copy ?? 'Bring the ambition. We’ll bring the strategy, craft, and technical firepower.' }}</p>
            <a class="btn btn-ink" href="{{ route('contact') }}">
                Start a conversation <i class="bi bi-arrow-up-right"></i>
            </a>
        </div>
    </div>
</section>
